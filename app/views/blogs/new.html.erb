<div class="write">
  <div class="publish">
	  <div class="con">
      <%= form_for :blog, url: blogs_path, html: {:id => 'blog-form'} do |f| %>
        <%= render partial: 'form', :object => f %>
      <% end %>
    </div>
  </div>
</div>